require("copilot").setup({
  suggestion = {
    auto_trigger = true,
    keymap = {
      -- change these if you want
      accept = "<C-j>",
      next = "<M-]>",
      prev = "<M-[>",
      -- dismiss = "<C-]>",
    },
  },
})
